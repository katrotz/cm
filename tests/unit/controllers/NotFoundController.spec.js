'use strict';

import NotFoundController from 'code/lib/controllers/NotFoundController.js';

describe('NotFoundController', function() {
  let ctrl;

  beforeEach(function() {
    ctrl = new NotFoundController();
  });

  //it('should be defined', function() {
  //  expect(ctrl).toBeTruthy();
  //});
});